/** Segmento dinámico: hijos usan useSearchParams / cliente. */
export const dynamic = 'force-dynamic';

export default function RegistroLayout({ children }: { children: React.ReactNode }) {
  return children;
}
